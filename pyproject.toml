[project]
name = "radicalized"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "caldav>=2.2.6",
    "python-dotenv>=1.2.1",
    "vobject>=0.9.9",
]
